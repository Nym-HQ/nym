export const preservedSubdomains = ['app', 'staging']

export const defaultBookmarkTags = ['website', 'reading', 'portfolio']

// These words can not be used for tags because they conflict with the already existing routes
export const forbiddenBookmarkTags = ['add', 'atom', 'feed', 'index', 'rss']
